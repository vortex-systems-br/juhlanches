<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Luh Lanches - Premium Street Food. Os melhores hambúrgueres e hot dogs da região. Peça agora pelo WhatsApp!">
    <title>Luh Lanches | Premium Street Food</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Montserrat:wght@700;800;900&display=swap" rel="stylesheet">
    
    <style>
        /* --- Variáveis Globais --- */
        :root {
            --bg-dark: #0a0a0c;
            --bg-card: #151518;
            --bg-card-hover: #1e1e23;
            --primary: #ff5500;
            --primary-glow: rgba(255, 85, 0, 0.6);
            --secondary: #ffb300;
            --text-light: #ffffff;
            --text-muted: #a0a0a5;
            --border-color: #2a2a30;
            --font-heading: 'Montserrat', sans-serif;
            --font-body: 'Inter', sans-serif;
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* --- Reset & Base --- */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; }
        body {
            background-color: var(--bg-dark);
            color: var(--text-light);
            font-family: var(--font-body);
            line-height: 1.6;
            overflow-x: hidden;
        }
        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }

        /* --- Header & Nav --- */
        .header {
            background: rgba(10, 10, 12, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--border-color);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            padding: 15px 0;
        }
        .header-content { display: flex; justify-content: space-between; align-items: center; }
        .logo { font-family: var(--font-heading); font-size: 1.5rem; font-weight: 900; letter-spacing: 1px; color: var(--secondary); }
        .logo span { color: var(--primary); text-shadow: 0 0 10px var(--primary-glow); }
        .nav-menu a {
            color: var(--text-light); text-decoration: none; margin-left: 25px;
            font-weight: 500; font-size: 0.95rem; transition: var(--transition);
        }
        .nav-menu a:hover { color: var(--primary); }

        /* --- Hero Section --- */
        .hero {
            height: 100vh; min-height: 600px; display: flex; align-items: center; position: relative;
            background: url('https://images.unsplash.com/photo-1550547660-d9450f859349?q=80&w=2000&auto=format&fit=crop') center/cover no-repeat;
            margin-top: 60px;
        }
        .hero-overlay {
            position: absolute; inset: 0;
            background: linear-gradient(to right, rgba(10, 10, 12, 0.95) 20%, rgba(10, 10, 12, 0.6) 100%);
        }
        .hero-content { position: relative; z-index: 10; max-width: 600px; }
        .glowing-text {
            font-family: var(--font-heading); font-size: 4.5rem; font-weight: 900; color: var(--primary);
            text-transform: uppercase; line-height: 1.1; margin-bottom: 10px;
            text-shadow: 0 0 20px var(--primary-glow), 0 0 40px var(--primary-glow);
        }
        .subtitle { font-family: var(--font-heading); color: var(--secondary); font-size: 1.2rem; letter-spacing: 3px; margin-bottom: 20px; }
        .description { color: var(--text-muted); font-size: 1.1rem; margin-bottom: 40px; }

        /* --- Botões --- */
        .cta-button, .btn-primary {
            display: inline-flex; align-items: center; gap: 10px;
            background: linear-gradient(45deg, var(--primary), #ff2a00);
            color: white; font-family: var(--font-heading); font-size: 1.1rem; font-weight: 700;
            padding: 16px 32px; border: none; border-radius: 8px; cursor: pointer;
            transition: var(--transition); box-shadow: 0 8px 25px rgba(255, 85, 0, 0.4);
            text-transform: uppercase; letter-spacing: 1px;
        }
        .cta-button:hover, .btn-primary:hover { transform: translateY(-3px); box-shadow: 0 12px 30px rgba(255, 85, 0, 0.6); }
        .pulse { animation: pulse 2s infinite; }
        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(255, 85, 0, 0.7); }
            70% { box-shadow: 0 0 0 15px rgba(255, 85, 0, 0); }
            100% { box-shadow: 0 0 0 0 rgba(255, 85, 0, 0); }
        }

        /* --- Sticky Categories --- */
        .sticky-categories {
            position: sticky; top: 60px; background: rgba(10, 10, 12, 0.9); backdrop-filter: blur(8px);
            border-bottom: 1px solid var(--border-color); z-index: 900; padding: 15px 0;
        }
        .categories-wrapper { display: flex; gap: 15px; overflow-x: auto; scrollbar-width: none; }
        .categories-wrapper::-webkit-scrollbar { display: none; }
        .category-pill {
            background: var(--bg-card); color: var(--text-light); text-decoration: none;
            padding: 8px 20px; border-radius: 50px; font-size: 0.9rem; font-weight: 600;
            border: 1px solid var(--border-color); white-space: nowrap; transition: var(--transition);
        }
        .category-pill.active, .category-pill:hover {
            background: rgba(255, 85, 0, 0.1); color: var(--primary); border-color: var(--primary);
        }

        /* --- Seções e Grid --- */
        .section-padding { padding: 80px 0; }
        .dark-bg { background-color: #0d0d0f; }
        .section-title { font-family: var(--font-heading); font-size: 2.5rem; margin-bottom: 40px; text-align: center; }
        .section-title span { color: var(--primary); }

        /* --- Destaque (Carro-Chefe) --- */
        .featured-product {
            display: grid; grid-template-columns: 1fr 1fr; gap: 50px; align-items: center;
            background: var(--bg-card); border-radius: 20px; padding: 40px; border: 1px solid var(--border-color);
        }
        .featured-image { position: relative; border-radius: 15px; overflow: hidden; }
        .featured-image img { width: 100%; height: auto; border-radius: 15px; display: block; transition: transform 0.5s ease; }
        .featured-image:hover img { transform: scale(1.05); }
        .badge-fire, .badge-new {
            position: absolute; top: 20px; left: 20px; padding: 6px 15px; border-radius: 4px;
            font-family: var(--font-heading); font-size: 0.8rem; font-weight: 800; letter-spacing: 1px; z-index: 2;
        }
        .badge-fire { background: var(--primary); color: white; box-shadow: 0 0 10px var(--primary-glow); }
        .badge-new { background: var(--secondary); color: var(--bg-dark); }
        .featured-info h2 { font-family: var(--font-heading); font-size: 2.5rem; margin-bottom: 15px; }
        .featured-desc { color: var(--text-muted); font-size: 1.1rem; margin-bottom: 30px; }
        .price-row { display: flex; align-items: center; gap: 30px; }
        .price { font-family: var(--font-heading); font-size: 2rem; color: var(--secondary); font-weight: 800; }

        /* --- Cards de Produto --- */
        .product-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; }
        .product-card {
            background: var(--bg-card); border-radius: 16px; overflow: hidden;
            border: 1px solid var(--border-color); transition: var(--transition);
            display: flex; flex-direction: column;
        }
        .product-card:hover { transform: translateY(-5px); border-color: rgba(255, 85, 0, 0.3); box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5); }
        .card-image { position: relative; height: 240px; overflow: hidden; }
        .card-image img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s ease; }
        .product-card:hover .card-image img { transform: scale(1.08); }
        .card-content { padding: 25px; flex-grow: 1; display: flex; flex-direction: column; }
        .card-content h3 { font-family: var(--font-heading); font-size: 1.3rem; margin-bottom: 10px; }
        .card-content p { color: var(--text-muted); font-size: 0.95rem; margin-bottom: 20px; flex-grow: 1; }
        .card-bottom { display: flex; justify-content: space-between; align-items: center; border-top: 1px solid var(--border-color); padding-top: 15px; }
        .card-bottom .price { font-size: 1.4rem; }
        .btn-icon {
            background: transparent; border: 1px solid var(--primary); color: var(--primary);
            width: 45px; height: 45px; border-radius: 8px; display: flex; align-items: center; justify-content: center;
            cursor: pointer; transition: var(--transition);
        }
        .btn-icon:hover { background: var(--primary); color: white; box-shadow: 0 0 15px var(--primary-glow); }

        /* --- Footer --- */
        .footer { background: #050505; border-top: 1px solid var(--border-color); padding: 60px 0 20px; }
        .footer-content { display: grid; grid-template-columns: 1fr 1fr; gap: 40px; margin-bottom: 40px; }
        .brand-footer p { color: var(--text-muted); margin-top: 15px; }
        .contact-info h4 { font-family: var(--font-heading); margin-bottom: 15px; color: var(--text-light); }
        .contact-info p { color: var(--text-muted); margin-bottom: 5px; }
        .footer-zap { display: inline-block; color: var(--primary); text-decoration: none; font-weight: 600; margin-top: 10px; font-size: 1.1rem; }
        .footer-bottom { text-align: center; color: var(--text-muted); font-size: 0.85rem; border-top: 1px solid var(--border-color); padding-top: 20px; }

        /* --- Animações --- */
        .fade-in { opacity: 0; transform: translateY(30px); transition: opacity 0.8s ease, transform 0.8s ease; }
        .fade-in.visible { opacity: 1; transform: translateY(0); }

        /* --- Media Queries (Responsivo) --- */
        @media (max-width: 900px) {
            .featured-product { grid-template-columns: 1fr; }
            .glowing-text { font-size: 3.5rem; }
        }
        @media (max-width: 768px) {
            .nav-menu { display: none; }
            .hero-content { text-align: center; }
            .price-row { flex-direction: column; align-items: flex-start; gap: 15px; }
            .footer-content { grid-template-columns: 1fr; text-align: center; }
        }
    </style>
</head>
<body>

    <header class="header">
        <div class="container header-content">
            <div class="logo">LUH<span>LANCHES</span></div>
            <nav class="nav-menu">
                <a href="#destaque">Carro-Chefe</a>
                <a href="#x-burgers">X-Burgers</a>
                <a href="#hot-dogs">Hot Dogs</a>
            </nav>
        </div>
    </header>

    <section class="hero" id="home">
        <div class="hero-overlay"></div>
        <div class="container hero-content fade-in">
            <h1 class="glowing-text">LUH LANCHES</h1>
            <p class="subtitle">PREMIUM STREET FOOD • EST. 2023</p>
            <p class="description">A verdadeira experiência do street food. Ingredientes selecionados, carnes suculentas e um sabor inesquecível.</p>
            <button class="cta-button pulse" onclick="orderOnWhatsApp('Olá! Gostaria de conhecer o cardápio da Luh Lanches.')">
                <span>🔥</span> Peça Agora no WhatsApp
            </button>
        </div>
    </section>

    <div class="sticky-categories">
        <div class="container categories-wrapper">
            <a href="#destaque" class="category-pill active">Destaque</a>
            <a href="#x-burgers" class="category-pill">X-Burgers</a>
            <a href="#hot-dogs" class="category-pill">Hot Dogs</a>
        </div>
    </div>

    <section class="section-padding" id="destaque">
        <div class="container">
            <div class="featured-product fade-in">
                <div class="featured-image">
                    <img src="https://images.unsplash.com/photo-1568901346375-23c9450c58cd?q=80&w=1000&auto=format&fit=crop" alt="Supremo Bacon Burger" loading="lazy">
                    <div class="badge-fire">MAIS PEDIDO</div>
                </div>
                <div class="featured-info">
                    <h2>Supremo Bacon Blaze</h2>
                    <p class="featured-desc">Nosso carro-chefe. Pão brioche selado na manteiga, duplo blend de Angus 160g, queijo cheddar derretido, fatias grossas de bacon artesanal crocante, cebola caramelizada e nosso molho secreto Luh.</p>
                    <div class="price-row">
                        <span class="price">R$ 38,90</span>
                        <button class="btn-primary" onclick="orderOnWhatsApp('Olá! Quero pedir um Supremo Bacon Blaze (R$ 38,90).')">Pedir Agora</button>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="section-padding dark-bg" id="x-burgers">
        <div class="container">
            <h2 class="section-title">Nossos <span>X-Burgers</span></h2>
            <div class="product-grid">
                
                <div class="product-card fade-in">
                    <div class="card-image">
                        <img src="https://images.unsplash.com/photo-1594212202875-546659f4d700?q=80&w=800&auto=format&fit=crop" alt="Classic Smash" loading="lazy">
                    </div>
                    <div class="card-content">
                        <h3>Classic Smash</h3>
                        <p>Pão brioche, 2 smash burgers 90g, duplo american cheese, picles e maionese da casa.</p>
                        <div class="card-bottom">
                            <span class="price">R$ 26,90</span>
                            <button class="btn-icon" onclick="orderOnWhatsApp('Quero pedir um Classic Smash (R$ 26,90)')" aria-label="Pedir no WhatsApp">
                                <svg width="24" height="24" viewBox="0 0 24 24" fill